---
interface Props {
	date: Date;
}

const { date } = Astro.props;
---

<time datetime={date.toISOString()}>
	{
		date.toLocaleDateString('en-ca', {
			month: '2-digit',
			year: 'numeric',
			day: 'numeric',
			
			// day: 'numeric',
		})
	}
</time>
